%row
  %div.large-4.large-centered.columns
    .titl
      %h2= @user.name
%row
  %div.large-4.large-centered.columns
    %table
      %thead
        %tr
          %th Role
          %th= @user.roles.first.name.titleize if @user.roles.any?
        %tr
          %th Email
          %th= @user.email
        %tr
          %th Created at
          %th= @user.created_at.to_date
    = link_to "Back", users_path, class: "button tiny radius"